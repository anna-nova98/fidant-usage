# Database — for SQLite use a file path, for PostgreSQL use a connection string
DATABASE_URL="file:./prisma/dev.db"
# DATABASE_URL="postgresql://user:password@localhost:5432/fidant"

# Auth — secret key for JWT token signing
SECRET_KEY="your-secret-key-here"
